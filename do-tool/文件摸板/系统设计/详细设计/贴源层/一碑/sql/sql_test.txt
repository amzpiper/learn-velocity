

-- 问题主表 --
DROP TABLE IF EXISTS `tbb_building_ques`;
CREATE TABLE IF NOT EXISTS `tbb_building_ques`
(
    `问题主表` 问题任务id primary key COMMENT 'questask_id',
    `问题主表` 任务提交时间 COMMENT 'submit_date',
    `问题主表` 创建时间 COMMENT 'create_date',
    `问题主表` 是否公共区域 COMMENT 'is_public',
    `问题主表` 开始时间 COMMENT 'start_date',
    `问题主表` 派单时间 COMMENT 'give_out_date',
    `问题主表` 派单时限 COMMENT 'give_out_date_line',
    `问题主表` 接单时间 COMMENT 'accept_date',
    `问题主表` 接单时限 COMMENT 'accept_date_line',
    `问题主表` 完成时间 COMMENT 'finish_date',
    `问题主表` 处理时限 COMMENT 'end_date',
    `问题主表` 关闭时间 COMMENT 'close_date',
    `问题主表` 关闭时限 COMMENT 'close_date_line',
    `问题主表` 任务类型 COMMENT 'bi_problem_category',
    `问题主表` 返工次数 COMMENT 'multimaintain_state',
    `问题主表` 操作时间 COMMENT 'opera_date',
    `问题主表` 更新时间 COMMENT 'update_date',
    `问题主表` 问题描述 COMMENT 'ques_desc',
    `问题主表` 任务状态 COMMENT 'qusetask_state',
    `问题主表` 承建商名称 COMMENT 'builder_name',
    `问题主表` 承建商id COMMENT 'builder_id',
    `问题主表` 关联楼栋id COMMENT 'building_id',
    `问题主表` 问题级别 COMMENT 'emergency_degree',
    `问题主表` 关联房间id COMMENT 'info_id',
    `问题主表` 受理人id COMMENT 'ori_userid',
    `问题主表` 最后一级任务分类id COMMENT 'server_id',
    `问题主表` 房间部位id COMMENT 'room_part_id',
    `问题主表` 部位 COMMENT 'room_part',
    `问题主表` 功能分区id COMMENT 'room_type_id',
    `问题主表` 更新者id COMMENT 'updater',
    `问题主表` 作废审批时间 COMMENT 'close_approval_date',
    `问题主表` 作废状态 COMMENT 'close_status',
    `问题主表` 作废提交时间 COMMENT 'close_submit_date',
    `问题主表` 中止期限 COMMENT 'pause_deadline',
    `问题主表` 中止状态 COMMENT 'pause_status',
    `问题主表` 中止审批时间 COMMENT 'pause_approval_date',
    `问题主表` 中止提交时间 COMMENT 'pause_submit_date',
    `问题主表` 类型名称 COMMENT 'type_name',
    `问题主表` 原因 COMMENT 'reason',
    `问题主表` 问题编码 COMMENT 'questask_code',
    `问题主表` 返修次数 COMMENT 'repair_num',
    `问题主表` 联系地址 COMMENT 'contact_address',
    `问题主表` 联系人 COMMENT 'contact_name',
    `问题主表` 联系电话 COMMENT 'contact_phone',
    `问题主表` 问题类型 COMMENT 'question_type',
    `问题主表` 业主id COMMENT 'owner_info_id',
    `问题主表` 各级分类拼接内容 COMMENT 'problem_tmp_name',
    `问题主表` 请求来源 COMMENT 'task_source',
    `问题主表` 任务类型 COMMENT 'task_type',
    `问题主表` 状态 COMMENT 'state',
    `问题主表` 中止作废申请人 COMMENT 'applicant',
    `问题主表` 预约时间 COMMENT 'appointment_date',
    `问题主表` 后续处理人id COMMENT 'followup_processor',
    `问题主表` 楼层 COMMENT 'house_floor',
    `问题主表` 项目id COMMENT 'project_id',
    `问题主表` 项目名称 COMMENT 'project_name',
    `问题主表` 回调地址 COMMENT 'revoke_url',
    `问题主表` 区域主键 COMMENT 'area_id',
    `问题主表` 区域名称 COMMENT 'area_name',
    `问题主表` 非正常关闭状态 COMMENT 'abnormity_close_status',
    `问题主表` 非正常关闭申请时间 COMMENT 'abnormity_close_submit_date',
    `问题主表` 非正常关闭审批通过 COMMENT 'abnormity_close_approval_date',
    `问题主表` 功能分区名称 COMMENT 'room_type',
    `问题主表` 部位名称 COMMENT 'room_part_name',
    `问题主表` 楼栋名称 COMMENT 'house_building',
    `问题主表` 单元 COMMENT 'house_cell',
    `问题主表` 房号 COMMENT 'house_room',
    `问题主表` 产品类型 COMMENT 'building_property',
    `问题主表` 受理人姓名 COMMENT 'orig_user_name',
    `问题主表` 处理人姓名 COMMENT 'followup_processor_name',
    `问题主表` 业主姓名 COMMENT 'owner_name',
    `问题主表` 中止作废原因 COMMENT 'reason_name',
    `问题主表` 中止原因 COMMENT 'delay_reason',
    `问题主表` 业主会员id COMMENT 'member_id',
    `问题主表` 质保开始时间 COMMENT 'warranty_start_date',
    `问题主表` 质保结束时间 COMMENT 'warranty_end_date',
    `问题主表` 父任务编号 COMMENT 'parent_questask_code',
    `问题主表` 父任务id COMMENT 'parent_id',
    `问题主表` 维修人 COMMENT 'maintenance_man',
    `问题主表` 派单人id COMMENT 'send_userid',
    `问题主表` 投诉渠道 COMMENT 'complaint_channel',
    `问题主表` 维修人id COMMENT 'mtain_userid',
    `问题主表` 评价（回访）方式 COMMENT 'visit_type',
    `问题主表` 回访状态 COMMENT 'visit_state',
    `问题主表` 是否需要材料 COMMENT 'is_need_material',
    `问题主表` app下单系统 COMMENT 'out_order_task_system',
    `问题主表` 地产任务id COMMENT 'dc_questask_id',
    `问题主表` 地产任务编号 COMMENT 'dc_questask_code',
    `问题主表` 回访评价满意度 COMMENT 'satisfied',
    `问题主表` 回访任务id COMMENT 'visit_id',
    `问题主表` 对外回复审批通过时间 COMMENT 'reply_approval_date',
    `问题主表` 对外回复申请时间 COMMENT 'reply_submit_date',
    `问题主表` 对外回复内容 COMMENT 'external_reply',
    `问题主表` 对外回复状态 COMMENT 'reply_review_status',
    `问题主表` 支付方式 COMMENT 'pay_type',
    `问题主表` 实际工时 COMMENT 'work_hour',
    `问题主表` 额定工时 COMMENT 'rated_work_hour',
    `问题主表` 费用 COMMENT 'price',
    `问题主表` 参考金额 COMMENT 'rated_amount',
    `问题主表` 材料费用 COMMENT 'material_price',
    `问题主表` 人工费用 COMMENT 'manmade_price',
    `问题主表` 应收id COMMENT 'receivable_id',
    `问题主表` 支付状态 COMMENT 'pay_status',
    `问题主表` 是否支付 COMMENT 'is_paid',
    `问题主表` 起单来源 COMMENT 'orign_source',
    `问题主表` 录单系统 COMMENT 'source_type',
    `问题主表` 维修分类id COMMENT 'cate_id',
    `问题主表` 关联的维修单id COMMENT 'mtain_taskid',
    `问题主表` 问题位置 COMMENT 'room_info',
    `问题主表` 是否房修单 COMMENT 'house_fix_status',
    `问题主表` 房修关闭状态 COMMENT 'housefix_close_status',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbb_building_ques(`问题主表`,``) values(0,0);
insert into tbb_building_ques(`问题主表`,``) values(1,1);
insert into tbb_building_ques(`问题主表`,``) values(2,2);
insert into tbb_building_ques(`问题主表`,``) values(3,3);
insert into tbb_building_ques(`问题主表`,``) values(4,4);
insert into tbb_building_ques(`问题主表`,``) values(5,5);
insert into tbb_building_ques(`问题主表`,``) values(6,6);
insert into tbb_building_ques(`问题主表`,``) values(7,7);
insert into tbb_building_ques(`问题主表`,``) values(8,8);
insert into tbb_building_ques(`问题主表`,``) values(9,9);


-- 维修协助人表 --
DROP TABLE IF EXISTS `tbb_ques_helper`;
CREATE TABLE IF NOT EXISTS `tbb_ques_helper`
(
    `维修协助人表` 表主键 primary key COMMENT 'id',
    `维修协助人表` 创建时间 COMMENT 'create_date',
    `维修协助人表` 工作量百分比 COMMENT 'percent',
    `维修协助人表` 关联任务id COMMENT 'task_id',
    `维修协助人表` 有效标识 COMMENT 'state',
    `维修协助人表` 协助人或者维修人id COMMENT 'user_id',
    `维修协助人表` 协助人或者维修人姓名 COMMENT 'user_name',
    `维修协助人表` 工作时长（分钟） COMMENT 'work_minutes',
    `维修协助人表` 区域id COMMENT 'area_id',
    `维修协助人表` 区域名称 COMMENT 'area_name',
    `维修协助人表` 项目id COMMENT 'project_id',
    `维修协助人表` 项目名称 COMMENT 'project_name',
    `维修协助人表` 人员类型 COMMENT 'user_type',
    `维修协助人表` 任务注来源 COMMENT 'orign_source',
    `维修协助人表` 任务编号 COMMENT 'task_code',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbb_ques_helper(`维修协助人表`,``) values(0,0);
insert into tbb_ques_helper(`维修协助人表`,``) values(1,1);
insert into tbb_ques_helper(`维修协助人表`,``) values(2,2);
insert into tbb_ques_helper(`维修协助人表`,``) values(3,3);
insert into tbb_ques_helper(`维修协助人表`,``) values(4,4);
insert into tbb_ques_helper(`维修协助人表`,``) values(5,5);
insert into tbb_ques_helper(`维修协助人表`,``) values(6,6);
insert into tbb_ques_helper(`维修协助人表`,``) values(7,7);
insert into tbb_ques_helper(`维修协助人表`,``) values(8,8);
insert into tbb_ques_helper(`维修协助人表`,``) values(9,9);


-- 核查任务 --
DROP TABLE IF EXISTS `tbg_quality_task`;
CREATE TABLE IF NOT EXISTS `tbg_quality_task`
(
    `核查任务` 主键id primary key COMMENT 'task_id',
    `核查任务` 计划id COMMENT 'plan_id',
    `核查任务` 计划名称 COMMENT 'task_name',
    `核查任务` 项目id COMMENT 'project_id',
    `核查任务` 创建日期 COMMENT 'create_date',
    `核查任务` 创建人 COMMENT 'creator',
    `核查任务` 修改日期 COMMENT 'operate_date',
    `核查任务` 操作人 COMMENT 'operator',
    `核查任务` 任务状态 COMMENT 'task_state',
    `核查任务` 任务状态 COMMENT 'handle_state',
    `核查任务` 开始时间 COMMENT 'start_date',
    `核查任务` 结束时间 COMMENT 'end_date',
    `核查任务` 数据状态 COMMENT 'state',
    `核查任务` 公司id COMMENT 'company_id',
    `核查任务` 任务实际开始时间 COMMENT 'check_time',
    `核查任务` 完成时间 COMMENT 'complete_time',
    `核查任务` 完成提交人id COMMENT 'submitter',
    `核查任务` 品质分类 COMMENT 'quality_category',
    `核查任务` 品质7 COMMENT 'lp_category',
    `核查任务` 核查总得分数 COMMENT 'score',
    `核查任务` 核查总分数 COMMENT 'score_total',
    `核查任务` 100*总得分数/总分数 COMMENT 'rate',
    `核查任务` 问题总数 COMMENT 'problem_num',
    `核查任务` 版本号___时间戳 COMMENT 'version',
    `核查任务` 任务描述 COMMENT 'task_desc',
    `核查任务` 任务关闭原因字典值 COMMENT 'close_reason',
    `核查任务` 任务原因备注 COMMENT 'close_remark',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_quality_task(`核查任务`,``) values(0,0);
insert into tbg_quality_task(`核查任务`,``) values(1,1);
insert into tbg_quality_task(`核查任务`,``) values(2,2);
insert into tbg_quality_task(`核查任务`,``) values(3,3);
insert into tbg_quality_task(`核查任务`,``) values(4,4);
insert into tbg_quality_task(`核查任务`,``) values(5,5);
insert into tbg_quality_task(`核查任务`,``) values(6,6);
insert into tbg_quality_task(`核查任务`,``) values(7,7);
insert into tbg_quality_task(`核查任务`,``) values(8,8);
insert into tbg_quality_task(`核查任务`,``) values(9,9);


-- 核查计划 --
DROP TABLE IF EXISTS `tbg_quality_plan`;
CREATE TABLE IF NOT EXISTS `tbg_quality_plan`
(
    `核查计划` 主键id primary key COMMENT 'plan_id',
    `核查计划` 计划名称 COMMENT 'plan_name',
    `核查计划` 核查标准(工艺路线)id COMMENT 'craft_id',
    `核查计划` 创建日期 COMMENT 'create_date',
    `核查计划` 创建人 COMMENT 'creator',
    `核查计划` 修改日期 COMMENT 'operate_date',
    `核查计划` 操作人 COMMENT 'operator',
    `核查计划` 开始时间 COMMENT 'start_date',
    `核查计划` 结束时间 COMMENT 'end_date',
    `核查计划` 数据状态 COMMENT 'state',
    `核查计划` 暂停状态 COMMENT 'pause_flag',
    `核查计划` 生成方式 COMMENT 'make_type',
    `核查计划` 公司id COMMENT 'company_id',
    `核查计划` 频次 COMMENT 'fre_type',
    `核查计划` 时间间隔 COMMENT 'interval_time',
    `核查计划` 计划详情 COMMENT 'plan_detail',
    `核查计划` 品质分类 COMMENT 'quality_category',
    `核查计划` 品质7 COMMENT 'lp_category',
    `核查计划` 是否验收签字 COMMENT 'need_sign',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_quality_plan(`核查计划`,``) values(0,0);
insert into tbg_quality_plan(`核查计划`,``) values(1,1);
insert into tbg_quality_plan(`核查计划`,``) values(2,2);
insert into tbg_quality_plan(`核查计划`,``) values(3,3);
insert into tbg_quality_plan(`核查计划`,``) values(4,4);
insert into tbg_quality_plan(`核查计划`,``) values(5,5);
insert into tbg_quality_plan(`核查计划`,``) values(6,6);
insert into tbg_quality_plan(`核查计划`,``) values(7,7);
insert into tbg_quality_plan(`核查计划`,``) values(8,8);
insert into tbg_quality_plan(`核查计划`,``) values(9,9);


-- 管护计划操作记录 --
DROP TABLE IF EXISTS `tbg_asset_plan_log`;
CREATE TABLE IF NOT EXISTS `tbg_asset_plan_log`
(
    `管护计划操作记录` 记录id primary key COMMENT 'log_id',
    `管护计划操作记录` 创建日期 COMMENT 'create_date',
    `管护计划操作记录` 创建人 COMMENT 'creator',
    `管护计划操作记录` 关联的计划id COMMENT 'plan_id',
    `管护计划操作记录` 内容 COMMENT 'remark',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(0,0);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(1,1);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(2,2);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(3,3);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(4,4);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(5,5);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(6,6);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(7,7);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(8,8);
insert into tbg_asset_plan_log(`管护计划操作记录`,``) values(9,9);


-- 管护任务 --
DROP TABLE IF EXISTS `tbg_asset_task`;
CREATE TABLE IF NOT EXISTS `tbg_asset_task`
(
    `管护任务` 主键id primary key COMMENT 'task_id',
    `管护任务` 计划id COMMENT 'plan_id',
    `管护任务` 资产id COMMENT 'asset_id',
    `管护任务` 创建日期 COMMENT 'create_date',
    `管护任务` 创建人 COMMENT 'creator',
    `管护任务` 修改日期 COMMENT 'operate_date',
    `管护任务` 操作人 COMMENT 'operator',
    `管护任务` 任务状态 COMMENT 'task_state',
    `管护任务` 开始时间 COMMENT 'start_date',
    `管护任务` 结束时间 COMMENT 'end_date',
    `管护任务` 检查地点，资产地址信息 COMMENT 'location',
    `管护任务` 数据状态 COMMENT 'state',
    `管护任务` 任务完成描述 COMMENT 'task_remake',
    `管护任务` 公司id COMMENT 'company_id',
    `管护任务` 完成时间 COMMENT 'complete_time',
    `管护任务` 完成提交人id COMMENT 'submitter',
    `管护任务` 品质分类 COMMENT 'quality_category',
    `管护任务` 品质7资产管护6 COMMENT 'lp_category',
    `管护任务` 任务实际开始时间 COMMENT 'check_time',
    `管护任务` 合格率（分数） COMMENT 'rate',
    `管护任务` pdf路径地址 COMMENT 'task_pdf',
    `管护任务` 版本号___时间戳 COMMENT 'version',
    `管护任务` 是否计入统计内容 COMMENT 'need_count',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_asset_task(`管护任务`,``) values(0,0);
insert into tbg_asset_task(`管护任务`,``) values(1,1);
insert into tbg_asset_task(`管护任务`,``) values(2,2);
insert into tbg_asset_task(`管护任务`,``) values(3,3);
insert into tbg_asset_task(`管护任务`,``) values(4,4);
insert into tbg_asset_task(`管护任务`,``) values(5,5);
insert into tbg_asset_task(`管护任务`,``) values(6,6);
insert into tbg_asset_task(`管护任务`,``) values(7,7);
insert into tbg_asset_task(`管护任务`,``) values(8,8);
insert into tbg_asset_task(`管护任务`,``) values(9,9);


-- 管护任务记录 --
DROP TABLE IF EXISTS `tbg_asset_task_record`;
CREATE TABLE IF NOT EXISTS `tbg_asset_task_record`
(
    `管护任务记录` 记录id primary key COMMENT 'id',
    `管护任务记录` 资产id COMMENT 'asset_id',
    `管护任务记录` 计划id COMMENT 'plan_id',
    `管护任务记录` 任务id COMMENT 'task_id',
    `管护任务记录` 所核查类型id COMMENT 'level_id',
    `管护任务记录` 核查记录json串 COMMENT 'fields_json',
    `管护任务记录` 追记描述json COMMENT 'recover_json',
    `管护任务记录` 问题描述 COMMENT 'record_detail',
    `管护任务记录` 提交人 COMMENT 'creator',
    `管护任务记录` 提交时间 COMMENT 'create_date',
    `管护任务记录` 问题状态 COMMENT 'question_state',
    `管护任务记录` 问题类型 COMMENT 'question_type',
    `管护任务记录` 是否是问题 COMMENT 'qualified_state',
    `管护任务记录` 完成时间 COMMENT 'submit_date',
    `管护任务记录` 提交人id COMMENT 'submitter',
    `管护任务记录` 公司id COMMENT 'company_id',
    `管护任务记录` 数据状态 COMMENT 'state',
    `管护任务记录` 品质分类 COMMENT 'quality_category',
    `管护任务记录` 记录状态 COMMENT 'record_state',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_asset_task_record(`管护任务记录`,``) values(0,0);
insert into tbg_asset_task_record(`管护任务记录`,``) values(1,1);
insert into tbg_asset_task_record(`管护任务记录`,``) values(2,2);
insert into tbg_asset_task_record(`管护任务记录`,``) values(3,3);
insert into tbg_asset_task_record(`管护任务记录`,``) values(4,4);
insert into tbg_asset_task_record(`管护任务记录`,``) values(5,5);
insert into tbg_asset_task_record(`管护任务记录`,``) values(6,6);
insert into tbg_asset_task_record(`管护任务记录`,``) values(7,7);
insert into tbg_asset_task_record(`管护任务记录`,``) values(8,8);
insert into tbg_asset_task_record(`管护任务记录`,``) values(9,9);


-- 管护任务记录检查标准详情 --
DROP TABLE IF EXISTS `tbg_asset_task_record_info`;
CREATE TABLE IF NOT EXISTS `tbg_asset_task_record_info`
(
    `管护任务记录检查标准详情` 主键 primary key COMMENT 'id',
    `管护任务记录检查标准详情` 任务记录id COMMENT 'task_record_id',
    `管护任务记录检查标准详情` 任务id COMMENT 'task_id',
    `管护任务记录检查标准详情` 分类id COMMENT 'level_id',
    `管护任务记录检查标准详情` 分类标准id COMMENT 'field_id',
    `管护任务记录检查标准详情` 状态 COMMENT 'field_state',
    `管护任务记录检查标准详情` 创建人 COMMENT 'creator',
    `管护任务记录检查标准详情` 修改时间 COMMENT 'create_date',
    `管护任务记录检查标准详情` 数据状态 COMMENT 'state',
    `管护任务记录检查标准详情` 公司id COMMENT 'company_id',
    `管护任务记录检查标准详情` 问题描述 COMMENT 'problem_detail',
    `管护任务记录检查标准详情` 经度 COMMENT 'longitude',
    `管护任务记录检查标准详情` 纬度 COMMENT 'latitude',
    `管护任务记录检查标准详情` 问题位置信息 COMMENT 'location',
    `管护任务记录检查标准详情` 问题状态 COMMENT 'question_state',
    `管护任务记录检查标准详情` 问题类型 COMMENT 'question_type',
    `管护任务记录检查标准详情` 完成时间 COMMENT 'submit_date',
    `管护任务记录检查标准详情` 完成人id COMMENT 'submitter',
    `管护任务记录检查标准详情` 审批状态 COMMENT 'approval_status',
    `管护任务记录检查标准详情` 整改措施 COMMENT 'measures',
    `管护任务记录检查标准详情` 追记记录来源 COMMENT 'info_path',
    `管护任务记录检查标准详情` 审批备注 COMMENT 'approval_remark',
    `管护任务记录检查标准详情` 隐患类型 COMMENT 'hidden_danger_type',
    `管护任务记录检查标准详情` 隐患类别 COMMENT 'hidden_danger_category',
    `管护任务记录检查标准详情` 隐患级别 COMMENT 'hidden_danger_level',
    `管护任务记录检查标准详情` 问题描述id COMMENT 'problem_id',
    `管护任务记录检查标准详情` 问题描述 COMMENT 'standard_description',
    `管护任务记录检查标准详情` 隐患等级 COMMENT 'hidden_danger_grade',
    `管护任务记录检查标准详情` 整改建议 COMMENT 'rectification_suggestions',
    `管护任务记录检查标准详情` 位置和人员信息json串 COMMENT 'location_info_json',
    `管护任务记录检查标准详情` 所有操作人 COMMENT 'operators',
    `管护任务记录检查标准详情` 地产oa的回传id COMMENT 'oa_id',
    `管护任务记录检查标准详情` 版本号___时间戳 COMMENT 'version',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(0,0);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(1,1);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(2,2);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(3,3);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(4,4);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(5,5);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(6,6);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(7,7);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(8,8);
insert into tbg_asset_task_record_info(`管护任务记录检查标准详情`,``) values(9,9);


-- 设备 --
DROP TABLE IF EXISTS `tzm_maintain_device`;
CREATE TABLE IF NOT EXISTS `tzm_maintain_device`
(
    `设备` 主键 primary key COMMENT 'device_id',
    `设备` 品牌名称 COMMENT 'brand_name',
    `设备` 楼址id COMMENT 'buidling_detail_id',
    `设备` 机房id COMMENT 'building_id',
    `设备` 路址详情 COMMENT 'building_locations',
    `设备` 巡更地点4 COMMENT 'building_type',
    `设备` 联系方式 COMMENT 'concat_way',
    `设备` 施工方 COMMENT 'construct_user',
    `设备` 删除状态 COMMENT 'state',
    `设备`  COMMENT 'device_address',
    `设备` 设备分类id COMMENT 'device_category_id',
    `设备` 设备编码 COMMENT 'device_code',
    `设备` 设备详情 COMMENT 'device_detail',
    `设备` 设备详细地址 COMMENT 'device_location',
    `设备` 设备名 COMMENT 'device_name',
    `设备` 设备唯一编号 COMMENT 'device_number',
    `设备` 设备状态设备状态 COMMENT 'device_state',
    `设备` 设备型号 COMMENT 'device_model',
    `设备` 报废日期 COMMENT 'disable_date',
    `设备` eba预警未处理数量(默认为0) COMMENT 'eba_caut_num',
    `设备`  COMMENT 'factory',
    `设备`  COMMENT 'fm_id',
    `设备`  COMMENT 'has_exce',
    `设备`  COMMENT 'ins_pos',
    `设备` 是否巡查点 COMMENT 'is_check',
    `设备` 是否报废 COMMENT 'is_disable',
    `设备` 最后一次维修时间 COMMENT 'last_maintain_time',
    `设备` 最后一次巡检时间 COMMENT 'last_patrol_time',
    `设备` 最后一次维保时间 COMMENT 'last_protect_time',
    `设备` 联系人 COMMENT 'link_man',
    `设备` 类型 COMMENT 'lp_category',
    `设备`  COMMENT 'model',
    `设备` 修改日期 COMMENT 'operate_date',
    `设备` 修改人 COMMENT 'operator',
    `设备`  COMMENT 'parameter1',
    `设备`  COMMENT 'parameter2',
    `设备` 质保年份 COMMENT 'pledge_year',
    `设备`  COMMENT 'power',
    `设备`  COMMENT 'project_id',
    `设备`  COMMENT 'receipts_id',
    `设备`  COMMENT 'service_dept',
    `设备` device的根节点设备的id，否则是自己 COMMENT 'sys_id',
    `设备` 时间戳 COMMENT 'time_stamp',
    `设备` 投入日期 COMMENT 'use_date',
    `设备`  COMMENT 'use_dept',
    `设备`  COMMENT 'use_name',
    `设备` 是否写入文件 COMMENT 'write_flag',
    `设备`  COMMENT 'point_id',
    `设备`  COMMENT 'city_id',
    `设备` 设备类型级别id COMMENT 'level_id',
    `设备` 规格型号id(tzm_device_norm) COMMENT 'dnorm_id',
    `设备` 机房id COMMENT 'device_partrol_id',
    `设备` 设备类型的id(tzm_device_type) COMMENT 'dtype_id',
    `设备` 父设备id COMMENT 'parent_id',
    `设备` 系统gid COMMENT 'p_sys_id',
    `设备`  COMMENT 'eq_description',
    `设备`  COMMENT 'fm_device_type',
    `设备`  COMMENT 'fm_status',
    `设备` 二维码文件名称 COMMENT 'qr_code_name',
    `设备` 二维码文件地址 COMMENT 'qr_code_path',
    `设备` 纬度 COMMENT 'latitude',
    `设备` 经度 COMMENT 'longitude',
    `设备`  COMMENT 'company_id',
    `设备` 设备标识，用于巡逻点辅助搜索 COMMENT 'device_mark',
    `设备` 出厂编号 COMMENT 'appearance_code',
    `设备` 出厂日期 COMMENT 'appearance_date',
    `设备` 设备参数 COMMENT 'device_param',
    `设备` 产地 COMMENT 'lacation',
    `设备` 维保详情 COMMENT 'maintain_record',
    `设备`  COMMENT 'model_name',
    `设备` 所涉范围 COMMENT 'scope',
    `设备` 使用日期 COMMENT 'using_date',
    `设备` 版本号 COMMENT 'timestamp_version',
    `设备` 制造上 COMMENT 'manufacturer',
    `设备` 面积 COMMENT 'square',
    `设备` 数量 COMMENT 'count',
    `设备`  COMMENT 'level_desc',
    `设备`  COMMENT 'risk',
    `设备` 风险等级 COMMENT 'risk_id',
    `设备`  COMMENT 'annual_inspectiontime',
    `设备`  COMMENT 'device_parameter',
    `设备`  COMMENT 'maintain_unit',
    `设备`  COMMENT 'over_rec',
    `设备` 注册编码 COMMENT 'registration_code',
    `设备`  COMMENT 'superior',
    `设备`  COMMENT 'disable_notice_date',
    `设备`  COMMENT 'fault_submit_time',
    `设备`  COMMENT 'fault_submit_user',
    `设备`  COMMENT 'is_intelligent_device',
    `设备` 智能设备是否在线 COMMENT 'is_online',
    `设备` 设备开始运行时间 COMMENT 'device_start_date_time',
    `设备` 设备预警时长 COMMENT 'device_warning_duration',
    `设备` 设备预警内容 COMMENT 'device_warning_info',
    `设备` 设备是否已经预警 COMMENT 'device_is_warning',
    `设备` 实际归还时间 COMMENT 'act_return_time',
    `设备` 是否可借资产 COMMENT 'borrowable_asset',
    `设备` 借出归还状态 COMMENT 'lend_status',
    `设备` 解除时间 COMMENT 'lend_time',
    `设备` 最后一次借出人 COMMENT 'lend_user',
    `设备` 归属管理员 COMMENT 'manager_id',
    `设备` 预计归还时间 COMMENT 'plan_return_time',
    `设备` 第一个借用人 COMMENT 'first_lend_user',
    `设备`  COMMENT 'is_can_return',
    `设备` 路址id COMMENT 'info_id',
    `设备` 是否可查看 COMMENT 'viewable',
    `设备` 预计报废时间 COMMENT 'expect_disable_date',
    `设备` 客户编码 COMMENT 'device_code_client',
    `设备`  COMMENT 'implementation_place',
    `设备` 是否重要 COMMENT 'is_important',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tzm_maintain_device(`设备`,``) values(0,0);
insert into tzm_maintain_device(`设备`,``) values(1,1);
insert into tzm_maintain_device(`设备`,``) values(2,2);
insert into tzm_maintain_device(`设备`,``) values(3,3);
insert into tzm_maintain_device(`设备`,``) values(4,4);
insert into tzm_maintain_device(`设备`,``) values(5,5);
insert into tzm_maintain_device(`设备`,``) values(6,6);
insert into tzm_maintain_device(`设备`,``) values(7,7);
insert into tzm_maintain_device(`设备`,``) values(8,8);
insert into tzm_maintain_device(`设备`,``) values(9,9);


-- 巡检计划 --
DROP TABLE IF EXISTS `tbg_device_patrol_rule`;
CREATE TABLE IF NOT EXISTS `tbg_device_patrol_rule`
(
    `巡检计划` 主键 primary key COMMENT 'patrol_rule_id',
    `巡检计划` 创建日期 COMMENT 'create_date',
    `巡检计划` 创建人 COMMENT 'creator',
    `巡检计划` 修改日期 COMMENT 'operate_date',
    `巡检计划` 修改人 COMMENT 'operator',
    `巡检计划` 删除状态 COMMENT 'state',
    `巡检计划` 机房id COMMENT 'address_id',
    `巡检计划` 自动化标志 COMMENT 'auto_flag',
    `巡检计划` 工艺路线 COMMENT 'craft_id',
    `巡检计划` 废弃，不再使用 COMMENT 'device_id',
    `巡检计划` 巡检抽样率 COMMENT 'device_sam_rate',
    `巡检计划` 是否抢单 COMMENT 'grab_flag',
    `巡检计划` 间隔 COMMENT 'inte_date',
    `巡检计划` 巡检等级id COMMENT 'level_prop_id',
    `巡检计划` 生成方式 COMMENT 'make_type',
    `巡检计划` 备注 COMMENT 'patrol_remark',
    `巡检计划` 计划名称 COMMENT 'patrol_rule_name',
    `巡检计划` 1主设备2子设备 COMMENT 'patrol_type',
    `巡检计划` 人数 COMMENT 'people_num',
    `巡检计划` 频次类型 COMMENT 'task_fre_type',
    `巡检计划` 1：常规任务；2：循环任务 COMMENT 'task_type',
    `巡检计划` 工时（分钟） COMMENT 'working_time',
    `巡检计划` 有效期 COMMENT 'eff_end_date',
    `巡检计划` 有效期 COMMENT 'eff_start_date',
    `巡检计划` 期望创建任务数 COMMENT 'expect_create_task_num',
    `巡检计划` 类型 COMMENT 'lp_category',
    `巡检计划` 巡检点间隔 COMMENT 'point_interval_time',
    `巡检计划` 专业 COMMENT 'professional',
    `巡检计划` 系统 COMMENT 'sys_id',
    `巡检计划`  COMMENT 'dms_rule_id',
    `巡检计划` 项目id COMMENT 'project_id',
    `巡检计划` 计划等级 COMMENT 'rank',
    `巡检计划` 详情 COMMENT 'detail',
    `巡检计划`  COMMENT 'distributeflag',
    `巡检计划` 公司id COMMENT 'company_id',
    `巡检计划`  COMMENT 'inspection',
    `巡检计划` 1.频次2.日历,3装修,4空置房,5工程装修 COMMENT 'plantype',
    `巡检计划` 标准设备id列表 COMMENT 'device_ids',
    `巡检计划` 是否按标准创建爱你 COMMENT 'hasstandard',
    `巡检计划` 标准负责人 COMMENT 'userid',
    `巡检计划`  COMMENT 'supervision_jobid',
    `巡检计划`  COMMENT 'supervision_jobname',
    `巡检计划` 是否限制抢单 COMMENT 'grab_limit_flag',
    `巡检计划`  COMMENT 'disaster_type_id',
    `巡检计划` 1_是,0_否;默认为1 COMMENT 'is_emergency',
    `巡检计划` 是否暂停 COMMENT 'pause_flag',
    `巡检计划` 导入编号 COMMENT 'transaction_id',
    `巡检计划`  COMMENT 'rule_rank',
    `巡检计划` 1_是,0_否;默认为0 COMMENT 'is_holiday',
    `巡检计划` 1_是,0_否;默认为0 COMMENT 'is_scanned_code',
    `巡检计划` 1_是,0_否;默认为0 COMMENT 'is_work_result',
    `巡检计划` 1_否,2_是;默认为1 COMMENT 'post_task',
    `巡检计划` 1_是,0_否;默认为1 COMMENT 'take_picture',
    `巡检计划` 装修计划id COMMENT 'decoration_id',
    `巡检计划` 废弃，不再使用 COMMENT 'patrol_interval',
    `巡检计划` 审核人id COMMENT 'audit_user',
    `巡检计划` 类别描述 COMMENT 'category_desc_id',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_device_patrol_rule(`巡检计划`,``) values(0,0);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(1,1);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(2,2);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(3,3);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(4,4);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(5,5);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(6,6);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(7,7);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(8,8);
insert into tbg_device_patrol_rule(`巡检计划`,``) values(9,9);


-- 巡检任务 --
DROP TABLE IF EXISTS `tbg_device_patrol_rule_task`;
CREATE TABLE IF NOT EXISTS `tbg_device_patrol_rule_task`
(
    `巡检任务` 主键 primary key COMMENT 'task_id',
    `巡检任务` 创建日期 COMMENT 'create_date',
    `巡检任务` 创建人 COMMENT 'creator',
    `巡检任务` 修改日期 COMMENT 'operate_date',
    `巡检任务` 修改人 COMMENT 'operator',
    `巡检任务` 删除状态 COMMENT 'state',
    `巡检任务` 是否循环任务 COMMENT 'cycle',
    `巡检任务`  COMMENT 'destroy_order_satisfy_desc',
    `巡检任务`  COMMENT 'destroy_order_satisfy_id',
    `巡检任务`  COMMENT 'destroy_user_id',
    `巡检任务` 废弃，不再使用 COMMENT 'device_id',
    `巡检任务` 设备id COMMENT 'device_ids',
    `巡检任务` 结束时间（日期） COMMENT 'end_date',
    `巡检任务`  COMMENT 'fields_json',
    `巡检任务` 完整的结束时间 COMMENT 'full_end_date',
    `巡检任务` 完成的开始时间 COMMENT 'full_start_date',
    `巡检任务`  COMMENT 'gtform_id',
    `巡检任务` 是否工单池任务 COMMENT 'is_workorder',
    `巡检任务` 任务初始负责人 COMMENT 'owner_id',
    `巡检任务` 巡检时间 COMMENT 'patrol_date',
    `巡检任务` 巡检状态 COMMENT 'patrol_state',
    `巡检任务` 人数 COMMENT 'people_num',
    `巡检任务`  COMMENT 'project_id',
    `巡检任务` 任务转发来源 COMMENT 'sour_id',
    `巡检任务` 开始时间（日期） COMMENT 'start_date',
    `巡检任务` 提交任务时间 COMMENT 'submit_date',
    `巡检任务` 任务详情 COMMENT 'task_detail',
    `巡检任务`  COMMENT 'task_source',
    `巡检任务` 任务状态 COMMENT 'task_state',
    `巡检任务` 负责人 COMMENT 'user_id',
    `巡检任务` 工时 COMMENT 'working_time',
    `巡检任务`  COMMENT 'point_id',
    `巡检任务` 计划id COMMENT 'patrol_rule_id',
    `巡检任务`  COMMENT 'fm_ids',
    `巡检任务` 任务类型 COMMENT 'lp_category',
    `巡检任务` 点位间隔时间（分钟） COMMENT 'point_interval_time',
    `巡检任务` 专业 COMMENT 'professional',
    `巡检任务` 拒单路径 COMMENT 'reject_path',
    `巡检任务` 系统 COMMENT 'system_id',
    `巡检任务` 片区id COMMENT 'team_id',
    `巡检任务` 同步时间 COMMENT 'server_time',
    `巡检任务`  COMMENT 'finish_check_type',
    `巡检任务` 任务完成时间 COMMENT 'finish_date',
    `巡检任务` 任务完成人 COMMENT 'finish_user_id',
    `巡检任务`  COMMENT 'last_finish_date',
    `巡检任务`  COMMENT 'last_finish_task_id',
    `巡检任务`  COMMENT 'next_finish_date',
    `巡检任务`  COMMENT 'next_finish_task_id',
    `巡检任务`  COMMENT 'complete_quality',
    `巡检任务`  COMMENT 'helpuserids',
    `巡检任务`  COMMENT 'integral',
    `巡检任务`  COMMENT 'maintenancegroup',
    `巡检任务`  COMMENT 'actual_working_time',
    `巡检任务`  COMMENT 'destroy_order_detail',
    `巡检任务`  COMMENT 'supervisionremindflag',
    `巡检任务` 申请关闭原因 COMMENT 'apply_close_reason',
    `巡检任务` 申请关闭备注 COMMENT 'apply_close_remark',
    `巡检任务` 关闭备注 COMMENT 'close_remark',
    `巡检任务` 任务状态 COMMENT 'task_state',
    `巡检任务` 版本号 COMMENT 'version',
    `巡检任务`  COMMENT 'auto_autoexpire_flag',
    `巡检任务`  COMMENT 'checked_device_ids',
    `巡检任务`  COMMENT 'company_id',
    `巡检任务` 是否申请关闭任务 COMMENT 'handle_state',
    `巡检任务` 是否工单池任务 COMMENT 'is_order',
    `巡检任务` 巡检计划名称 COMMENT 'patrol_rule_name',
    `巡检任务` 机房名称 COMMENT 'device_patrol_name',
    `巡检任务`  COMMENT 'area_id',
    `巡检任务` 装修计划id COMMENT 'decoration_id',
    `巡检任务` 是否审核 COMMENT 'is_audit',
    `巡检任务` 审核状态 COMMENT 'audit_state',
    `巡检任务` 拒单次数 COMMENT 'refuse_num',
    `巡检任务` 是否超时完成（ COMMENT 'overtime_finish',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT '',
    ``  COMMENT ''
);

insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(0,0);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(1,1);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(2,2);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(3,3);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(4,4);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(5,5);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(6,6);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(7,7);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(8,8);
insert into tbg_device_patrol_rule_task(`巡检任务`,``) values(9,9);
